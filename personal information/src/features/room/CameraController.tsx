import { useFrame, useThree } from '@react-three/fiber'
import { useEffect, useRef } from 'react'
import { MathUtils, Vector3 } from 'three'
import { CAMERA } from './roomItems'
export function CameraController(){const{camera,gl}=useThree();const pointer=useRef({x:0,y:0});const zoom=useRef(1);useEffect(()=>{const move=(e:PointerEvent)=>{const r=gl.domElement.getBoundingClientRect();pointer.current.x=((e.clientX-r.left)/r.width-.5)*2;pointer.current.y=((e.clientY-r.top)/r.height-.5)*2};const wheel=(e:WheelEvent)=>{e.preventDefault();zoom.current=MathUtils.clamp(zoom.current+e.deltaY*.00055,.65,1.14)};gl.domElement.addEventListener('pointermove',move);gl.domElement.addEventListener('wheel',wheel,{passive:false});return()=>{gl.domElement.removeEventListener('pointermove',move);gl.domElement.removeEventListener('wheel',wheel)}},[gl]);useFrame((_,delta)=>{const target=new Vector3(...CAMERA.target);const offset=new Vector3(...CAMERA.position).sub(target);offset.applyAxisAngle(new Vector3(0,1,0),MathUtils.degToRad(pointer.current.x*10));const right=new Vector3().crossVectors(offset,new Vector3(0,1,0)).normalize();offset.applyAxisAngle(right,MathUtils.degToRad(pointer.current.y*3.5));offset.multiplyScalar(zoom.current);camera.position.lerp(target.clone().add(offset),1-Math.exp(-4*delta));camera.lookAt(target)});return null}
